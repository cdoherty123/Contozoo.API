using System;
using System.Collections.Generic;
using System.Linq;
using Microsoft.AspNetCore.Mvc;
using Xunit;
using Moq;
using Contozoo.API.Animals;
using Contozoo.API.Interfaces;
using Contozoo.API.Repositories;

namespace ContozooApi.Test
{
    //public class AnimalsControllerTest
    //{
     //   [Fact]

        /*public async void GetReturnOkResult()
        {
                //arrange
                var mockRepo = new <IContozooRepository>();
                mockRepo.Setup(n => n.GetAll()).Returns(mockRepo.GetTestAnimalItems());
                var controller = new AnimalsController(mockRepo.Object);

                //act
                var result = controller.GetContozooAnimals();

                //assert
                var viewResult = Assert.IsType<ViewResult>(result);
                var viewResultAnimals = Assert.IsAssignableFrom<List<ContozooAnimal>>(viewResult.ViewAnimals.Model);
                Assert.Equal(5, viewResultAnimals.count); 
        */
    

}
